#ifndef _Editor_H
#define _Editor_H

/*The editor opens the desired text file and reads the data.
The data is saved in an internal linked list.
The command prompt is shown with a star.
The user enters a command. 
Eventual changes are made to the linked list.
When the user chooses "save" an output stream will
put the data from the linked list into the text file.*/

class Editor{
//function definitions
//member declarations
public:
Editor();
void start();


private:



};


#endif
